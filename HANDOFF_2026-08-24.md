# Tayari — Session Handoff (2026-08-24)

**Purpose:** End-to-end record of one long working session, for whoever (human or agent) picks this up next. Read this before touching anything — it will save you from re-discovering three environmental traps the hard way.

**Current HEAD:** `c6e0114` (7 commits ahead of `origin/main` as of this writing — see §3). **Nothing pushed.** All work is local, uncommitted-to-remote, per standing instruction to never push without explicit ask.

---

## 1. What this session actually did

Two connected bodies of work, in order:

1. **Executed the go/no-go staging evidence campaign** against the live local Docker stack (real commands, real output — see `lessons.md` entries from earlier today for the full backup/restore drill, RLS probes, cross-account IDOR test, etc.).
2. **Ran a ruthless sweep for one specific bug class** — "an operation fails and the code either says nothing or fabricates plausible-looking content instead of the truth" — across the entire stack: Python AI engine, Go gateway, React frontend, browser extension, and finally the Python test suite and E2E suite themselves. Found and fixed **~32 real bugs**, all with regression tests or live verification, all logged in detail in `lessons.md`. Then picked up a second handoff document (`RUTHLESS_USER_SERVING_PRODUCTIONIZATION_PROGRAM_2026-08-24.md`, written by a concurrent session) and closed out its entire **Phase 0** (QE-001 through QE-005, UX-001).

**The authoritative record of every individual fix is `lessons.md`.** It has ~10 dated entries from today, each with: what was found, root cause, fix, and a reusable lesson. This document is the *summary and current-state* view; `lessons.md` is the *detailed evidence*. Don't duplicate-read — if you need the exact diff reasoning for a specific fix, grep `lessons.md` for the file name.

---

## 2. Current verified state (as of HEAD `c6e0114`)

Run these yourself before trusting this table — it was true a few minutes ago, but "trust, verify" applies to this document too.

| Suite | Result | Command |
|---|---|---|
| Python (plain, no path filter) | **932 passed, 4 skipped, 0 failed** | `cd backend/python && JWT_SECRET="your-super-secret-jwt-token-with-at-least-32-characters-long" PYTHONPATH=. .venv/bin/pytest -q` |
| Go | **261 passed**, build+vet clean | `cd backend/go && go build ./... && go vet ./... && go test ./...` |
| Frontend unit | **177 passed** | `bun run test` (from repo root) |
| Frontend typecheck | clean | `npx tsc --noEmit -p .` |
| Frontend build | clean | `bun run build` |
| **E2E (full Playwright suite)** | **55 passed, 0 failed, 0 skipped** | see exact invocation in §4 — **do not run bare `npx playwright test`**, see §5.1 |
| `golangci-lint` errcheck | 47 non-issues (idiomatic Go), 3 real bugs fixed | `golangci-lint run --enable-only=errcheck ./...` (from `backend/go/`) |

Docker stack: all 17 containers up and healthy (`docker compose ps`). `go-backend` and `frontend` were both rebuilt today (`docker compose build go-backend frontend`) and are running images that include every fix in this session — **if you pull more changes and don't rebuild, you will be testing stale code and waste time chasing phantom bugs, exactly like I did (see §5.2).**

---

## 3. The 7 commits this session made, in order

All on `main`, all local-only (not pushed). Read each commit message for full detail; this is the one-line index.

```
20d78f8  Fix AI fabrication-on-failure across 7 engine modules; fix live recruiter-intel crash
df44b7f  Fix RecruiterOutreach silent failure, missing Go proxy route, dream_score fabrication
2edecb6  Fix silent-failure frontend bugs found by sweeping the empty catch{} pattern
7aee0c7  Fix 7 more silent-failure bugs: components/hooks catches + AgentReachHub fake-data fallbacks
e0c82cb  Fix 7 more false-success/silent-failure bugs: Go errcheck findings + extension autofill
1051fe8  Fix Python test collection collision and two non-deterministic agent-squad tests
c6e0114  Fix permanent llm_complete signature bug (QE-003); get E2E suite fully green
```

Two of these are worth knowing about specifically because they're the highest-impact:

- **`20d78f8`** found that 7 separate AI-backed endpoints (recruiter outreach, salary negotiation, live interview copilot, voice interview streaming, career-ops evaluation, pattern analysis, strategic analysis) all shared one bug shape: call the LLM, discard the real response, always serve a hardcoded template — or catch *any* exception (not just "LLM not configured") and fabricate content instead of propagating the failure. `CompanyRadar.tsx` did the same thing on the frontend, falling back to fake Stripe/OpenAI job listings.
- **`c6e0114`** is the payoff of removing those fabrication fallbacks: with the mask gone, discovered that **Live Interview Copilot has crashed on literally every single invocation since it was written** — `llm_complete(prompt=..., system_prompt=...)`, keyword arguments that don't exist on the real function. It now genuinely works (live-verified: real LLM-generated STAR content, 200 response).

---

## 4. How to actually run the full E2E suite (read this before you run it)

```bash
# 1. Make sure Docker is up and everything is healthy
docker compose ps   # all should say "healthy" or "Up"

# 2. Rebuild the frontend if you've touched any src/ file since the last build
#    (the E2E suite reuses the RUNNING container, not a fresh dev server —
#    see §5.1 for why this matters)
docker compose build frontend && docker compose up -d frontend

# 3. Run with the SAME flags CI's real authenticated E2E job uses —
#    NOT the bare `npx playwright test` default (see §5.1)
PLAYWRIGHT_REUSE_EXISTING_SERVER=true \
TAYARI_E2E_TEST_MODE=true \
E2E_TEST_PASSWORD="$(openssl rand -hex 24)" \
npx playwright test --reporter=list
```

If you only need one spec file, swap the last line for `npx playwright test e2e/<file>.spec.ts --reporter=list`.

---

## 5. Two environmental traps I fell into — don't repeat them

### 5.1 `playwright.config.ts`'s default `webServer` command is a trap for full-suite runs

It hardcodes fake stub values (`VITE_SUPABASE_URL=https://ci.example.supabase.co`, `VITE_API_URL=https://api.example.com/api`) copied from CI's `public_routes.spec.ts`-only job (which genuinely doesn't need a backend). If you run bare `npx playwright test`, Playwright spins its own dev server with those fake values, and **every test that does a real UI-driven signup/login will hang until timeout** — the frontend has nowhere real to send auth calls. This looks exactly like "the registration flow is broken" and is not. Always use the flags in §4. There's also an orphaned `.env.e2e` file at repo root with the *correct* values that nothing actually sources — dead documentation, don't trust it's wired to anything, but its values are correct if you want a reference.

### 5.2 Two servers on the same port produce genuinely inconsistent test results

Mid-session, Docker Desktop died from a machine-sleep event, and separately I'd manually started a debug `pnpm dev --port 8083` process that I forgot to kill. Both ended up bound to port 8083 simultaneously (macOS allows this under some socket conditions), and traffic routed unpredictably between them — including serving raw unbuilt `/src/*.tsx` from the stray dev server instead of the real built bundle. This produced *flaky, contradictory* results: the same test passed in isolation and failed in a full run. If you see that pattern, run `lsof -i :8083` (and `:8085`, `:8002`) before you suspect the app.

---

## 6. What's genuinely done vs. what's still open

### Done (Phase 0 of `RUTHLESS_USER_SERVING_PRODUCTIONIZATION_PROGRAM_2026-08-24.md`, in full)

- QE-001 (Python test collection collision) — fixed
- QE-002 (non-deterministic agent-squad tests) — fixed, and the underlying orchestrator's fail-closed design was already correct (the tests were wrong)
- QE-003 (Live Interview Copilot E2E) — fixed the *actual* bug (permanent crash), not just the test
- QE-004 (CTA accessibility contract) — already resolved by an earlier concurrent-session commit (`9368025`), confirmed via live test run
- QE-005 (429/401 noise in E2E) — no unexpected 429/401s observed across the full 55-test clean run; not separately deep-dived beyond that
- UX-001 (mobile nav Escape/focus) — already resolved by the same earlier commit, confirmed

### Explicitly NOT done — these are real, scoped, and next

From the productionization document's own structure, everything past Phase 0:

- **REL-001** — cut an immutable release SHA/tag from this exact commit, push, build images, capture digests. This is a **decision**, not a fix — I did not do this unilaterally and you probably shouldn't either without the user's explicit go-ahead on tagging/pushing.
- **Section 15 findings** (the "specialist-guided omission pass" — these are real, cited-to-specific-code findings, not speculation):
  - **DATA-006 (P0)** — `backend/go/internal/api/routes_account.go`'s account deletion can return HTTP 200 `{"status":"deleted"}` even when the GoTrue auth-user deletion *and* its direct-SQL fallback both fail (only logged, not surfaced). A user can be told they're deleted when they're not.
  - **REL-002 (P0)** — the staging evidence verifier accepts synthetic/placeholder attestations (example domains, all-zero hashes, `development`/`staging-hostile-verification` environment labels) as if they were real deployment proof.
  - **CAP-001 (P0)** — `workspace.task_control` is always-on in the capability matrix even when all autonomous/external capabilities are disabled; the safety invariant ("it can't reach submission") is currently a code comment, not an executable proof.
  - **DATA-007 (P1)** — account data export silently substitutes `[]` for a category whose query failed, indistinguishable from "genuinely empty."
  - **DATA-008 (P1)** — `src/lib/telemetry-scrub.ts` (the file this session built earlier today) only redacts top-level keys by name; nested objects/arrays and truncated-not-redacted console messages can still leak resume/JD content.
  - **OPS-007 (P1)** — the backup/restore drill (this session verified it works, see `lessons.md`) only covers the `public` Postgres schema, not auth/storage/config/secrets — not a complete recovery proof.
  - **OPS-008 (P1)**, **AUTO-001 (P1)**, **REL-003 (P2)** — live-provider-readiness-as-release-evidence, worker/scheduler idempotency proof, SBOM/provenance auditability. Read the source document's §15 for full detail on each; it's thorough and specific.

None of these were touched this session. They range from "a clean, bounded bug fix" (DATA-006, DATA-008) to "needs a real architectural decision" (CAP-001, REL-002).

---

## 7. Files changed this session (for a quick `git diff` orientation)

Backend Python: `outreach_copilot.py`, `negotiation_copilot.py`, `live_interview_copilot.py`, `voice_stream.py`, `career_ops_evaluator.py`, `pattern_analyzer.py`, `strategic_analyzer.py`, `recruiter_intelligence.py`, `career_ops_routes.py`, `main.py`, plus 8 new/renamed test files.

Backend Go: `routes_one_stop.go` (new proxy route), `routes_api_keys.go`, `routes_mvp.go` (unchecked-error fixes).

Frontend: `ResumeUpload.tsx`, `main.tsx` + new `telemetry-scrub.ts`/`.test.ts`, `NegotiationCopilot.tsx`, `RecruiterOutreach.tsx`, `Omnisave.tsx`, `AgentReachHub.tsx`, `Pipeline.tsx`, `CompanyRadar.tsx`, `PreferenceProfileCard.tsx`, `JobFeedbackButtons.tsx`, `useDashboardData.ts`.

Extension: `background.js`, `content.js`.

E2E: `all_features.spec.ts`, `credit_billing_and_candidate_flow.spec.ts`.

`lessons.md`: ~10 new dated entries, one per fix batch, each with root cause + reusable lesson.

---

## 8. Standing rules that applied all session (keep applying them)

From `CLAUDE.md` / this session's actual behavior, not aspirational:

- **Never commit unless explicitly asked** — I did commit this session because the user explicitly said "commit" partway through. Never push without being asked; nothing here has been pushed.
- **Never fabricate a passing result.** Every "PASS" claim in this document and in `lessons.md` has real command output behind it. Where something couldn't be verified (e.g., the Go proxy-route fix, before the container was rebuilt), it was reported as "compile-verified only" rather than claimed as proven.
- **Read before write, minimal diff, no drive-by refactors.** Every fix this session is scoped to the actual bug, not a surrounding cleanup.
- **A missing test is a flagged gap, not a silent skip.** Several fixes note explicitly "no test added, here's why" rather than pretending coverage exists.
