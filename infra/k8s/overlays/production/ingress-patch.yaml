apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tayari-public
spec:
  tls:
    - hosts:
        - app.job-tayari.example.invalid
      secretName: prod-tayari-public-tls
  rules:
    - host: app.job-tayari.example.invalid
