appId: app.tayari.desktop
productName: Tayari Desktop
copyright: Copyright © 2026 Tayari
artifactName: ${productName}-${version}-${arch}.${ext}
asar: true
compression: maximum
files:
  - dist/**
  - electron/**
  - package.json
  - '!**/*.map'
  - '!electron/**/*.test.cjs'
  - '!electron/*.plist'
extraMetadata:
  version: 0.1.0
mac:
  category: public.app-category.productivity
  hardenedRuntime: true
  gatekeeperAssess: true
  strictVerify: true
  entitlements: electron/entitlements.plist
  entitlementsInherit: electron/entitlements.mac.plist
  target:
    - target: dmg
      arch:
        - arm64
    - target: zip
      arch:
        - arm64
  artifactName: ${productName}-${version}-${arch}.${ext}
extendInfo:
  NSMicrophoneUsageDescription: Tayari uses the microphone only when you start a voice-coaching session.
dmg:
  title: Tayari Desktop
  sign: true
notarize:
  teamId: ${env.APPLE_TEAM_ID}
