# Live UI observations — 2026-08-24

## Environment

- Inspected the locally running application on the connected computer at `http://127.0.0.1:8083`.
- Docker was listening on the expected application ports: frontend `8083`, Go gateway `8085`, Python AI `8002`, and Supabase gateway `8000`.
- The local working tree is on `main`, four commits ahead of `origin/main`; those four local commits are not part of the GitHub main branch as of this audit.

## Landing page

- The homepage is visually cohesive and professional: a restrained dark visual system, clear hierarchy, a focused career-operations proposition, and visible human-control language.
- The marketing copy explicitly distinguishes illustrative examples from real submissions and explains that important actions need review.
- The primary promise is now a deliberate, reviewable workflow rather than high-volume autonomous applications. This is clearer and safer than auto-apply marketing, although it is intentionally more conservative than a full "Manus Computer" claim.

## Resume Optimizer

- The live `/resume` interface accepts PDF/DOCX uploads, pasted job descriptions, and an optional public job-post URL import.
- It also exposes custom instructions and a clear warning that login, CAPTCHA, robots, and access controls will not be bypassed.
- The UI tells users to review every suggestion before using it.
- The page was accessible while signed in, but displayed the text “Sign in to save your results and access your history”; this needs investigation because it could be a UI/session-state inconsistency.
- No resume or job description was submitted during this audit to avoid altering the user's account data.

## Browser constraint

- A later attempt to navigate to `/onboarding` in the connected browser failed because the browser integration connection could not be re-established. Source-code evidence was used for the remaining analysis rather than making further browser interactions.

## Interpretation

- The visual presentation is substantially more professional than a prototype and the copy is mostly trustworthy.
- Live visual inspection alone does not establish that a backend workflow is functional; the final assessment must rely on the backend code and test results as well.

## Runtime and validation update

- The connected-computer shell could not reach the previously observed local ports `8083`, `8085`, or `8002`; its current `docker compose` project had no running `python-ai` service. The Docker containers running on the machine were named `mukthiguru-*`, not this repository's declared services. Therefore no authenticated end-to-end run of the current job, Gmail, browser-agent, or OmniSave backends was possible from this audit.
- A later browser attempt to revisit the local OmniSave page failed because the connected browser receiver was unavailable. This is an environment/connectivity limitation, not evidence that the feature works or fails.
- Automated validations completed locally: frontend unit tests `49 files / 177 tests` passed (with React `act(...)` warnings); Go backend `go test ./...` passed; focused Python safety and OmniSave suite `58 tests` passed (one Starlette deprecation warning); production frontend build succeeded; ESLint completed with `0 errors, 392 warnings` (mostly explicit `any`); the static security scan reported no new findings.
- Local `.env` had blank `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET`, so Gmail is unconfigured in this local setup and cannot be proven operational.
